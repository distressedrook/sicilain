    import XCTest
    @testable import Sicillian

    final class SicillianTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Sicillian().text, "Hello, World!")
        }
    }
